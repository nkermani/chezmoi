new_tab [ 1 - yazi ]
cd ~/Documents/dev/
launch zsh -ic "yazi; exec zsh"

new_tab [ 2 - opencode ]
cd ~
launch zsh -ic "opencode; exec zsh"

new_tab [ 3 - shell ]
# plain terminal, no auto command
launch zsh -ic "exec zsh"

new_tab [ 4 - notes ]
launch zsh -ic "cd ~/.nkermani/notes; exec zsh"

new_tab [ 5 - chezmoi ]
launch zsh -ic "chezmoi cd; exec zsh"

# Additional tabs that ensure a single instance is started (does nothing if already running)
# new_tab [ 6 - firefox ]
# launch zsh -ic "pgrep -x firefox >/dev/null || firefox >/dev/null 2>&1 &; exit"

# new_tab [ 7 - vscode ]
# launch zsh -ic "pgrep -x code >/dev/null || code --no-sandbox >/dev/null 2>&1 &; exit"

