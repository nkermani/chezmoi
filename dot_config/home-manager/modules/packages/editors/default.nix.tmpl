{
  imports = [
    ./fresh.nix
    ./helix.nix
    {{- if not (or (eq .chezmoi.os "darwin") (and (eq .chezmoi.os "linux") (contains "microsoft" .chezmoi.kernel.osrelease)))}}
      ./code.nix
      ./neovide.nix
      ./zed.nix
    {{- end }}
  ];
}
